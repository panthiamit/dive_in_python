def simp_intr(p,r,t) : 
    si = 0
    si = float(p*r*t//100)
    return si
temp = simp_intr(5000,6,5)
print('simple intrest for given data = ',temp)

# pass the p = princle amount , r = rate of intrest per year , t = time duration